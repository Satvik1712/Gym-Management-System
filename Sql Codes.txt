CREATE TABLE  "ANTHROPOMETRICS" 
   (	"ANTHROPOMETRIC_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"ANTHROPOMETRICS_DATE" DATE, 
	"WEIGHT" NUMBER(2,10), 
	"HEIGHT" NUMBER(2,10), 
	"WAIST_LENGTH" NUMBER(2,10), 
	"HIP_LENGTH" NUMBER(2,10), 
	"BICEP_LENGTH" NUMBER(2,10), 
	"CHEST_LENGTH" NUMBER(2,10), 
	 CONSTRAINT "ANTHROPOMETRICS_PK" PRIMARY KEY ("ANTHROPOMETRIC_ID") ENABLE, 
	 CONSTRAINT "ANTHROPOMETRICS_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE
   )

CREATE TABLE  "ATTENDANCE" 
   (	"ATTENDANCE_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"MEMBERSHIP_ID" NUMBER NOT NULL ENABLE, 
	"ATTENDANCE_DATE" DATE, 
	"ATTENDANCE_STATUS" VARCHAR2(40), 
	 CONSTRAINT "ATTENDANCE_PK" PRIMARY KEY ("ATTENDANCE_ID") ENABLE, 
	 CONSTRAINT "ATTENDANCE_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE, 
	 CONSTRAINT "ATTENDANCE_FK2" FOREIGN KEY ("MEMBERSHIP_ID")
	  REFERENCES  "MEMBERSHIP" ("MEMBERSHIP_ID") ENABLE
   )

CREATE TABLE  "AUTHENTICATION" 
   (	"ADMIN_ID" NUMBER NOT NULL ENABLE, 
	"ADMIN_PASSWORD" VARCHAR2(10) NOT NULL ENABLE, 
	 CONSTRAINT "AUTHENTICATION_PK" PRIMARY KEY ("ADMIN_ID") ENABLE
   )
CREATE TABLE  "CREDENTIALS" 
   (	"USER_ID" VARCHAR2(15) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(10) NOT NULL ENABLE, 
	 CONSTRAINT "CREDENTIALS_PK" PRIMARY KEY ("USER_ID") ENABLE
   )
CREATE TABLE  "CUSTOMER" 
   (	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"USER_ID" VARCHAR2(15) NOT NULL ENABLE, 
	"CUSTOMER_NAME" VARCHAR2(50), 
	"CUSTOMER_GENDER" VARCHAR2(10), 
	"CUSTOMER_EMAIL" VARCHAR2(30), 
	"CUSTOMER_ADDRESS" VARCHAR2(4000), 
	"CUSTOMER_DOB" DATE, 
	"CUSTOMER_MOBILENO" NUMBER, 
	"CUSTOMER_AADHARNO" VARCHAR2(15), 
	 CONSTRAINT "CUSTOMER_PK" PRIMARY KEY ("CUSTOMER_ID") ENABLE, 
	 CONSTRAINT "CUSTOMER_FK" FOREIGN KEY ("USER_ID")
	  REFERENCES  "CREDENTIALS" ("USER_ID") ENABLE
   )
CREATE TABLE  "EQUIPMENT" 
   (	"EQUIPMENT_ID" NUMBER NOT NULL ENABLE, 
	"EQUIPMENT_NAME" VARCHAR2(40), 
	"QUANTITY" NUMBER, 
	"WORKING_COUNT" NUMBER, 
	"NOTWORKING_COUNT" NUMBER, 
	"USE" VARCHAR2(100), 
	 CONSTRAINT "EQUIPMENT_PK" PRIMARY KEY ("EQUIPMENT_ID") ENABLE
   )
CREATE TABLE  "EQUIPMENT_MAINTENANCE" 
   (	"MAINTENANCE_ID" NUMBER NOT NULL ENABLE, 
	"EQUIPMENT_ID" NUMBER NOT NULL ENABLE, 
	"MAINTENANCE_DATE" DATE, 
	"MAINTENANCE_TYPE" VARCHAR2(40), 
	"MAINTENANCE_DETAILS" VARCHAR2(40), 
	"EXPENSE_ID" NUMBER, 
	 CONSTRAINT "EQUIPMENT_MAINTENANCE_PK" PRIMARY KEY ("MAINTENANCE_ID") ENABLE, 
	 CONSTRAINT "EQUIPMENT_MAINTENANCE_FK" FOREIGN KEY ("EQUIPMENT_ID")
	  REFERENCES  "EQUIPMENT" ("EQUIPMENT_ID") ENABLE, 
	 FOREIGN KEY ("EXPENSE_ID")
	  REFERENCES  "EXPENSES" ("EXPENSE_ID") ENABLE
   )

CREATE TABLE  "EXPENSES" 
   (	"EXPENSE_ID" NUMBER NOT NULL ENABLE, 
	"TRAINER_ID" NUMBER, 
	"MAINTENANCE_ID" NUMBER, 
	"EXPENSE_TYPE" VARCHAR2(40), 
	"EXPENSE_DATE" DATE, 
	"EXPENSE_AMOUNT" NUMBER, 
	"EXPENSE_METHOD" VARCHAR2(40), 
	"EXPENSE_STATUS" VARCHAR2(40), 
	"TRANSACTION_ID" NUMBER, 
	 CONSTRAINT "EXPENSES_PK" PRIMARY KEY ("EXPENSE_ID") ENABLE, 
	 CONSTRAINT "EXPENSES_FK" FOREIGN KEY ("TRAINER_ID")
	  REFERENCES  "TRAINER" ("TRAINER_ID") ENABLE, 
	 CONSTRAINT "EXPENSES_FK2" FOREIGN KEY ("MAINTENANCE_ID")
	  REFERENCES  "EQUIPMENT_MAINTENANCE" ("MAINTENANCE_ID") ENABLE
   )

CREATE TABLE  "FEEDBACK" 
   (	"FEEDBACK_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"RATING" NUMBER(2,10), 
	"FEEDBACK_DESCRIPTION" VARCHAR2(100), 
	 CONSTRAINT "FEEDBACK_PK" PRIMARY KEY ("FEEDBACK_ID") ENABLE, 
	 CONSTRAINT "FEEDBACK_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE
   )
CREATE TABLE  "MEMBERSHIP" 
   (	"MEMBERSHIP_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"PLAN_ID" NUMBER NOT NULL ENABLE, 
	"START_DATE" DATE, 
	"END_DATE" DATE, 
	"DETAILS" VARCHAR2(40), 
	"STATUS" VARCHAR2(40), 
	"PAYMENT_ID" NUMBER, 
	 CONSTRAINT "MEMBERSHIP_PK" PRIMARY KEY ("MEMBERSHIP_ID") ENABLE, 
	 CONSTRAINT "MEMBERSHIP_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE, 
	 CONSTRAINT "MEMBERSHIP_FK2" FOREIGN KEY ("PLAN_ID")
	  REFERENCES  "PLANS" ("PLAN_ID") ENABLE, 
	 FOREIGN KEY ("PAYMENT_ID")
	  REFERENCES  "PAYMENT" ("PAYMENT_ID") ENABLE
   )
CREATE TABLE  "ORDER_DETAILS" 
   (	"ORDER_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"PAYMENT_ID" NUMBER NOT NULL ENABLE, 
	"ITEMS" VARCHAR2(400), 
	"ORDER_DATE" DATE, 
	"DELIVERY_DATE" DATE, 
	"DELIVER_STATUS" VARCHAR2(40), 
	 CONSTRAINT "ORDER_DETAILS_PK" PRIMARY KEY ("ORDER_ID") ENABLE, 
	 CONSTRAINT "ORDER_DETAILS_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE, 
	 CONSTRAINT "ORDER_DETAILS_FK2" FOREIGN KEY ("PAYMENT_ID")
	  REFERENCES  "PAYMENT" ("PAYMENT_ID") ENABLE
   )
CREATE TABLE  "PAYMENT" 
   (	"PAYMENT_ID" NUMBER NOT NULL ENABLE, 
	"MEMBERSHIP_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"AMOUNT" NUMBER NOT NULL ENABLE, 
	"METHOD" VARCHAR2(40), 
	"TRANSACTION_ID" VARCHAR2(30) NOT NULL ENABLE, 
	"STATUS" VARCHAR2(10), 
	"INVOICE_NO" VARCHAR2(20) NOT NULL ENABLE, 
	"TYPE" VARCHAR2(4000), 
	 CONSTRAINT "PAYMENT_PK" PRIMARY KEY ("PAYMENT_ID") ENABLE, 
	 CONSTRAINT "PAYMENT_FK" FOREIGN KEY ("MEMBERSHIP_ID")
	  REFERENCES  "MEMBERSHIP" ("MEMBERSHIP_ID") ENABLE, 
	 CONSTRAINT "PAYMENT_FK2" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE
   )
CREATE TABLE  "PLANS" 
   (	"PLAN_ID" NUMBER NOT NULL ENABLE, 
	"PLAN_NAME" VARCHAR2(20) NOT NULL ENABLE, 
	"PLAN_DURATION" NUMBER NOT NULL ENABLE, 
	"PLAN_PRICE" NUMBER NOT NULL ENABLE, 
	"PLAN_SESSIONSPERWEEK" NUMBER NOT NULL ENABLE, 
	"PLAN_DESCRIPTION" VARCHAR2(25), 
	 CONSTRAINT "PLANS_PK" PRIMARY KEY ("PLAN_ID") ENABLE
   )

CREATE TABLE  "PRESENCE" 
   (	"PRESENCE_ID" NUMBER NOT NULL ENABLE, 
	"TRAINER_ID" NUMBER NOT NULL ENABLE, 
	"PRESENCE_DATE" DATE NOT NULL ENABLE, 
	"PRESENCE_STATUS" VARCHAR2(40), 
	"LOGIN_TIME" TIMESTAMP (6), 
	"LOGOUT_TIME" TIMESTAMP (6), 
	 CONSTRAINT "PRESENCE_PK" PRIMARY KEY ("PRESENCE_ID") ENABLE, 
	 CONSTRAINT "PRESENCE_FK" FOREIGN KEY ("TRAINER_ID")
	  REFERENCES  "TRAINER" ("TRAINER_ID") ENABLE
   )
CREATE TABLE  "SCHEDULE" 
   (	"SCHEDULE_ID" NUMBER NOT NULL ENABLE, 
	"CUSTOMER_ID" NUMBER NOT NULL ENABLE, 
	"TRAINER_ID" NUMBER NOT NULL ENABLE, 
	"TIMESLOT_ID" NUMBER NOT NULL ENABLE, 
	"SCHEDULE_DAY" VARCHAR2(40), 
	 CONSTRAINT "SCHEDULE_PK" PRIMARY KEY ("SCHEDULE_ID") ENABLE, 
	 CONSTRAINT "SCHEDULE_FK" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER" ("CUSTOMER_ID") ENABLE, 
	 CONSTRAINT "SCHEDULE_FK2" FOREIGN KEY ("TRAINER_ID")
	  REFERENCES  "TRAINER" ("TRAINER_ID") ENABLE, 
	 CONSTRAINT "SCHEDULE_FK3" FOREIGN KEY ("TIMESLOT_ID")
	  REFERENCES  "TIME_SLOT" ("TIMESLOT_ID") ENABLE
   )
CREATE TABLE  "TIME_SLOT" 
   (	"TIMESLOT_ID" NUMBER NOT NULL ENABLE, 
	"DAY" VARCHAR2(20) NOT NULL ENABLE, 
	"START_TIME" TIMESTAMP (6), 
	"END_TIME" TIMESTAMP (6), 
	 CONSTRAINT "TIME_SLOT_PK" PRIMARY KEY ("TIMESLOT_ID") ENABLE
   )
CREATE TABLE  "TRAINER" 
   (	"TRAINER_ID" NUMBER NOT NULL ENABLE, 
	"ADMIN_ID" NUMBER NOT NULL ENABLE, 
	"TRAINER_NAME" VARCHAR2(40), 
	"TRAINER_GENDER" VARCHAR2(10), 
	"TRAINER__SPECIALIZATION" VARCHAR2(40), 
	"TRAINER_EMAIL" VARCHAR2(40), 
	"TRAINER_DOB" DATE, 
	"TRAINER_MOBILENO" NUMBER, 
	"TRAINER_AADHARNO" NUMBER, 
	"TRAINER_SALARY" NUMBER, 
	 CONSTRAINT "TRAINER_PK" PRIMARY KEY ("TRAINER_ID") ENABLE, 
	 CONSTRAINT "TRAINER_FK" FOREIGN KEY ("ADMIN_ID")
	  REFERENCES  "AUTHENTICATION" ("ADMIN_ID") ENABLE
   )